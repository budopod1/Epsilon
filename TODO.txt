TODO:
* refactor all matchers into patternmatchers with custom pattern processors
* refactor methods in Compiler.cs to use PerformMatching
* remove unnecessary Copy()ing
* used IParentToken more often in matchers for iterating over children
* add compilation exceptions
* make VarDeclaration no longer a TreeToken, but a IParentToken
* replace TreeToken.Traverse with TokenUtils.Traverse
* change VarDeclaration tokenization
* change the:
        foreach (IToken token in program) {
            if (token is Function) {
                Function function = ((Function)token);
                Block block = function.GetBlock();
    structure into a system that will iterate over recursively nested blocks
* use custom internal errors
* add support for nested blocks
* filter to prevent FunctionRuleMatchers with duplicate IPatternSegments
* compute function return values
* reorder compiler methods

DONE:
