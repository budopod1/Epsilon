#import math;

Z#main {
    println["For (a+bi)^p:"];
    print["a? "];
    Q:a = parse_float[read_input_line];
    print["b? "];
    Q:bi = parse_float[read_input_line];
    print["p? "];
    Q:p = parse_float[read_input_line];
    C:z = C [a, bi];
    C:result = [z] ** [p];
    println[[[[[result.real].stringify] + [" + "]] + [[result.imag].stringify]] + ["i"]];
    return 0;
}
